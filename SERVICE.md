# SERVICE · 해외여행플래너

## 1. 사용자·문제·가치
- 누구를 위한 서비스인가: 세계여행을 가고 싶지만 예산·동반인·목적은 있어도 "어느 나라로, 어떻게" 갈지는 못 정한 사람. 동반인 유형(혼자·커플·가족·효도여행·친구)에 따라 알맞은 목적지와 일정이 완전히 달라지는데, 기존 여행 추천은 이를 반영하지 못한다. 예를 들어 "6살 여자아이를 데리고 어디를 가는 게 좋을까?"(가족), "부모님 모시고 효도여행 가고 싶은데 무리 없는 코스가 어디일까?"(효도여행), "여자친구랑 로맨틱한 커플 여행 가고 싶어"(커플)는 전부 "동반인이 있는 여행"이지만 정답이 완전히 다른 고민이다.
- 어떤 문제를 푸는가: 국가 선택부터 일자별 코스·예산 산정까지, 여행 계획 전체를 처음부터 직접 짜기엔 시간과 정보가 너무 많이 든다. 특히 동반인 유형(아동·시니어 동반 여부 등)에 따라 이동 거리·휴식 시간·활동 강도·안전 우선순위가 완전히 달라지는데, 기존 여행 정보는 이런 세부 조건까지 반영해주지 않는다
- 이 에이전트 없을 때는 블로그·커뮤니티·여행 카페를 오가며 국가 후보를 좁히고, 각 국가별로 다시 코스·예산을 일일이 리서치해야 한다. 이 서비스는 예산·동반인 구성·여행 목적 정도의 러프한 조건만 입력하면 (1) AI가 조건에 맞는 국가를 추천하거나 직접 탐색하게 해주고, (2) 선택한 국가 기준으로 동반인 유형에 맞춘(예: 아동 동반 시 낮잠 시간·키즈메뉴, 효도여행이면 여유로운 저강도 일정, 커플이면 함께 즐기기 좋은 코스) Day별 코스와 예산까지 자동으로 완성해준다.
- 이미 갈 곳(국가/도시)을 정한 사용자도 그대로 쓸 수 있다. 이 경우 국가 추천 단계를 건너뛰고 곧바로 그 목적지의 코스·동선·예산으로 들어간다.

## 2. 서비스 확장 관점
- 이 에이전트가 어떻게 돈을 벌 수 있는가: 미니 PJT 단계라 수익화는 목표가 아님 — 포트폴리오/코스 추천 품질 검증에 집중. v2로 실서비스화한다면 항공·숙소 예약 플랫폼과의 예약 연동 제휴 커미션(B2C) 모델을 우선 고려
- 규모: 개인 사용자가 여행 준비 시기마다 비정기적으로 사용하는 소규모 트래픽에서 시작. 확장 시 다수 사용자 동시 이용으로 트래픽 증가
- 대체제와의 차별점: 기존 여행 추천 서비스는 사용자가 국가를 먼저 정한 뒤 정보를 검색해야 하지만, 이 서비스는 국가 선택 자체를 AI 스코어링(추천 이유 카드 포함)으로 대신해줄 수 있고, "LLM 초안 + 지도 API 검증"을 결합해 근거 있는 Day별 코스와 예산을 자동 완성한다

## 3. 사용 예상 도구·데이터
> 도구가 여러 개로 늘어나 있어, 제출 범위를 우선순위로 나눠 관리한다. **국가 후보는 이미 큐레이션해 둔 인기 국가 목록(10~20개) 안에서 예산/목적으로 좁히는 정도로 충분하다고 보고, 이번 제출의 핵심은 그 안에서의 "장소 검증"과 "코스 생성" 품질이다.** 동선 최적화는 이미 구현은 해 뒀지만 시간이 허락되는 한도 내에서 붙이는 부가 기능으로 취급하고, 핵심 두 가지가 부실해지면서까지 우선하지 않는다.

**[P0 · 핵심] 장소 검증 + 코스 생성**
- 목적지 확정 시 국가 추천 건너뛰기: 사용자가 이미 국가/도시를 정해서 말하면 국가 추천(스코어링) 단계를 거치지 않고 곧바로 그 목적지의 코스 생성으로 들어간다. 목적지가 큐레이션 목록 밖이면(장소 검증 데이터가 없으면), 검증된 데이터가 없다는 점을 답변 맨 앞에서 먼저 밝히고 이후 내용은 일반 지식 기반 참고용임을 표시한다
- 코스 생성 (Claude API): 큐레이션된 인기 국가/도시 기준 Day 1, Day 2... 오전/오후/저녁 슬롯에 명소·액티비티·식당을 배치한 초안 작성. 사용자가 미리 저장(찜)해 둔 관광지·식당이 있으면 해당 장소를 고정 앵커로 먼저 배치하고, 나머지 빈 슬롯만 LLM이 동반인 연령·성별 조건까지 고려해 자동으로 채움
- 장소 검증 (Google Places API / 로컬 큐레이션 데이터): LLM이 생성한 장소명·좌표·영업시간·평점을 실제 데이터로 검증·보정. 코스에 들어가는 모든 장소는 반드시 이 검증을 거친 것만 사용

**[P0 · 지원] 국가 후보 필터링 (가벼운 수준)**
- 국가 후보 필터링: 이미 좁혀 둔 인기 국가 목록 안에서 예산(1차 필터)과 여행 목적·동반인 특성으로 상위 몇 개를 고르는 정도의 단순 로직. 정교한 스코어링 알고리즘을 더 정교하게 다듬는 데는 추가 시간을 쓰지 않는다
- 동반인 유형별 일정 보정: 동반인 구성에 따라 국가 스코어링과 코스 생성 로직을 다르게 적용 — 아동(예: 미취학) 동반 시 낮잠/휴식 슬롯 자동 삽입·이동 거리와 활동 강도 하향 조정·유모차 접근성/키즈메뉴 우선 필터링, 효도여행(부모님·어르신 동반) 시 안전도 가중치 상향·저강도 일정 우선, 커플 여행 시 함께 즐기기 좋은 활동·맛집 위주 구성. 친구·혼자 여행은 여행 목적 위주로 자유롭게 구성
- 찜 장소 반영: 사용자가 다른 화면에서 미리 저장한 관광지·식당 목록을 코스 생성 화면에서 바로 조회해 원하는 Day/슬롯에 추가할 수 있게 함 (AI 추천과 사용자 지정 장소가 하나의 코스 안에 공존)

**[P1 · 시간 허락 시] 동선 최적화**
- 동선 최적화: 하루 안에 사용자가 찜한 장소를 여러 곳 추가하면, 방문 순서를 사용자가 넣은 순서 그대로가 아니라 이동 거리·소요 시간이 최소가 되도록 자동으로 재배열. 단, 사용자가 순서를 수동으로 바꾸면 그 이후에는 자동 재배열하지 않고 사용자가 정한 순서를 그대로 유지. **핵심 기능은 아니므로, 장소 검증·코스 생성이 먼저 안정화된 뒤에만 붙인다**

**[보조] 나머지 편의 기능**
- 구글맵 연동 표시: 코스에 배치된 장소마다 별점·리뷰수를 화면에 노출하고 구글맵 링크를 연결. 상세 리뷰 텍스트는 서비스 내에서 직접 보여주지 않고, 링크를 통해 구글맵에서 확인하도록 함
- 계절 적합도 안내: 사용자가 입력한 여행 시기(월, 선택 사항)를 기준으로 각 추천 국가의 건기/우기를 판정해 국가 카드에 표시. 우기에 해당하면 비추천 사유와 함께 가까운 건기 시즌을 대안으로 안내
- 예산 시뮬레이션 로직: 숙소/교통/식비/액티비티 항목별 대략적 비용 추정

**데이터**
- Google Places API (실제 데이터) — 장소 좌표/영업시간/리뷰 평점, 아동 동반 시에는 제공되는 경우 유모차 접근성·키즈메뉴 여부 등 부가 속성도 함께 조회
- 국가별 메타데이터(평균 물가, 추천 시즌, 안전도 등) — 직접 큐레이션 대신 공개 데이터로 자동화: 외교부 해외안전여행 국가/지역 정보(안전도), Numbeo류 물가 지수(평균 물가), LLM 지식(추천 시즌 등 정성적 정보)을 조합. **현재 확보된 국가는 11곳**(베트남·태국·대만·일본·싱가포르·필리핀·말레이시아·인도네시아·괌·홍콩·미국 서부) — 이 목록 밖의 목적지는 검증된 장소 데이터 없이 참고용으로만 답변. **단, 실제 장소(코스 생성용) 데이터는 이 중 4곳(베트남·태국·대만·미국 서부)만 확보돼 있어, 나머지 7곳은 국가 추천까지는 되지만 코스 생성 요청 시 장소 데이터가 없다고 정직하게 안내됨** — 장소 데이터 확충은 v2 과제
- LLM(Claude) 여행 지식 기반 코스 초안 — 그 자체는 생성 데이터이며, 최종 노출 전 Google Places API로 검증됨
- 국가/지역별 월별 기후 데이터(건기·우기 구분) — 공개 기후 데이터(각국 기상청, Climate-Data.org류) 기반. 위 11개국 각각에 12개월치가 함께 확보되어 있음

## 4. 서비스 정책 (가드레일 요약)
- LLM이 생성한 장소·좌표·영업시간·가격은 지도/장소 API로 검증되기 전까지 최종 코스에 노출하지 않는다. 구조적으로 `list_candidate_places`/`verify_place`는 큐레이션 데이터나 실제 API 결과만 돌려주므로, 애초에 검증 안 된 값을 반환할 수 없다
- 예산 추정치는 실시간 가격이 아닌 평균값 기반 추정임을 사용자에게 명시적으로 고지한다
- 국가 추천/코스 생성의 근거가 불충분한 경우, 근거 없는 추천을 지어내지 않고 정보가 부족하다는 점을 먼저 밝힌다
- 질문에 개인정보(여권번호·카드번호·전화번호) 패턴이 있으면 모델을 호출하지 않고 그 자리에서 즉시 차단한다 (`PiiGuardrailMiddleware`, 코드 레벨로 강제)
- 프롬프트 인젝션이 의심되는 입력은 차단하지 않되, 감사 로그만 남기고 시스템 규칙을 그대로 유지한 채 응답한다 (`InjectionLogMiddleware`) — 오탐으로 정상 요청을 막는 것을 피하기 위한 의도적 설계이며, 실제로 규칙이 지켜지는지는 매 응답에서 시스템 프롬프트가 항상 다시 전달되는 구조로 담보한다
- 안전도가 낮은 지역(외교부 여행경보 상위 단계 등) 요청은 추천을 거절하지 않되, 위험성을 답변 맨 앞에서 먼저 안내한다 — 특히 아동 동반인 경우 위험성을 더 명확히 알린다
- 검증 대상 케이스는 `evaluation/test_queries.csv`에서 확인 가능: 개인정보·프롬프트 인젝션은 `guardrail` 카테고리(2건), 근거 없는 확정 요구·큐레이션 밖 목적지 등 나머지 정책은 `negative`(2건)·`edge`(3건) 카테고리에서 검증됨

## 5. 성공 기준
- `evaluation/test_queries.csv`의 케이스(12건 · positive 5(42%) · negative 2(17%) · edge 3(25%) · guardrail 2(17%)) 중 최소 90% 이상 통과. 이 중 장소 검증·코스 생성 관련 케이스(P0)를 우선 통과시키는 것을 최소 목표로 한다
- "쓸만하다"의 기준: 큐레이션된 인기 국가 안에서 Day별 코스에 들어간 장소가 전부 실제로 검증된 곳인지(지어낸 장소가 없는지), 그 코스를 그대로 따라가도 될 만큼 신뢰가 가는가
